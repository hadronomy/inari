apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: inari

resources:
  - namespace.yaml

helmGlobals:
  chartHome: ../../helm

helmCharts:
  - name: inari
    releaseName: inari
    namespace: inari
    valuesFile: values.yaml
    includeCRDs: false
    skipHooks: true
