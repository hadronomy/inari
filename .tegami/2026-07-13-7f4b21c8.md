---
packages:
  "group:edge": patch
  "group:controller-chart": patch
---

### Introduce Inari Device Center for Windows

The first Windows distribution packages Device Center as the user-session tray application and the Inari agent as its own delayed-start service. The signed MSIX includes protocol activation, protected local pairing, native credential storage, canonical brand assets, checksums, an SBOM, and installation guidance for managed environments.

### Establish recoverable release publishing

Tegami now versions the edge and controller chart independently while keeping each release group internally synchronized. Helm charts publish to GHCR with keyless signatures, Windows artifacts attach to the corresponding GitHub release, and interrupted publication can safely resume from verified remote state.
